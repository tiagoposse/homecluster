helmRepos:
  stable: https://charts.helm.sh/stable
  hashicorp: https://helm.releases.hashicorp.com
  jetstack: https://charts.jetstack.io
  nginx: https://kubernetes.github.io/ingress-nginx
  bananaspliff: https://bananaspliff.github.io/geek-charts
  drone: https://charts.drone.io
  gitea: https://dl.gitea.io/charts/
  # linkerd: https://helm.linkerd.io/stable
  kdash: https://kubernetes.github.io/dashboard

namespaces:
  default:
  tools:
  cert-manager:
  ingress:
  media:
  drone:
  builds:
  # linkerd:

apps:
  # linkerd:
  #   namespace: linkerd
  #   enabled: false
  #   chart: linkerd/linkerd2
  #   valuesFile: values/linkerd.yml
  #   version: 0.1.0

  # dashboard:
  #   namespace: tools
  #   enabled: true
  #   chart: kdash/kubernetes-dashboard
  #   version: 3.0.0
  #   valuesFile: values/dashboard.yml
  #   hooks:
  #     postInstall: resources/dashboard.yml

  # registry:
  #   namespace: tools
  #   enabled: true
  #   chart: stable/docker-registry
  #   valuesFile: values/registry.yml
  #   version: 1.9.6
  #   priority: -1000

  # ingress:
  #   namespace: ingress
  #   enabled: true
  #   chart: nginx/ingress-nginx
  #   valuesFile: values/ingress.yml
  #   version: 3.7.1
  #   priority: -1000
  #   hooks:
  #     preInstall: resources/reqs.yml

  # cert-manager:
  #   namespace: cert-manager
  #   enabled: true
  #   chart: jetstack/cert-manager
  #   valuesFile: values/vault.yml
  #   version: v1.0.4
  #   priority: -999
  #   hooks:
  #     postInstall: https://github.com/jetstack/cert-manager/releases/download/v1.0.4/cert-manager.crds.yaml

  # vault:
  #   namespace: tools
  #   enabled: true
  #   chart: hashicorp/vault
  #   valuesFile: values/vault.yml
  #   version: 0.8.0
  #   priority: -998
  #   hooks:
  #     preInstall: resources/vault_pre.yml
  #     postInstall: resources/vault_post.yml

  # dyndns:
  #   namespace: tools
  #   enabled: true
  #   chart: ./dyndns
  #   valuesFile: values/dyndns.yml
  #   version: 0.1.0
  #   priority: -499

  # media:
  #   namespace: media
  #   enabled: true
  #   chart: bananaspliff/emby
  #   valuesFile: values/media.yml
  #   version: 0.1.0
  #   priority: -499
  #   hooks:
  #     preInstall: resources/media-storage.yml

  drone:
    namespace: drone
    enabled: true
    chart: ../charts/charts/drone
    valuesFile: values/drone.yml
    version: 0.1.7
    hooks:
      preInstall: resources/drone.yml
    priority: -499

  drone-runner:
    namespace: builds
    enabled: true
    chart: ../charts/charts/drone-runner-kube
    valuesFile: values/drone-runner.yml
    version: 0.1.4
    priority: -498