namespace: tools
chart: vault
url: https://helm.releases.hashicorp.com
valuesFile: vault/values.yml
version: 0.8.0
pre:
  resources:
    - resources/vault_pre.yml
post:
  resources:
    - resources/vault_post.yml
