namespace: vault
release: vault
chart: vault
url: https://helm.releases.hashicorp.com
valuesFile: values.yml
version: 0.8.0
hooks:
  preInstall:
    - resource: k8s/auth-setup.yml
      operation: apply